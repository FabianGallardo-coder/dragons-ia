# Dragons & IA - Variables de entorno de ejemplo
# Copiá este archivo a .env y ajustá los valores

# --- Base de datos ---
# SQLite (desarrollo local)
DATABASE_URL=sqlite+aiosqlite:///./dragons_ia.db
# PostgreSQL (producción / Docker Compose)
# DATABASE_URL=postgresql+asyncpg://dragons:dragonspass@localhost:5432/dragons_ia

# --- JWT (OBLIGATORIO en producción, mínimo 32 caracteres) ---
# En desarrollo se genera uno aleatorio si no se proporciona
JWT_SECRET_KEY=tu_clave_secreta_de_al_menos_32_caracteres_aqui
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=1440

# --- IA ---
# Default model for local Ollama (low resource usage)
DEFAULT_AI_MODEL=ollama/dolphin-mistral:7b-v2.6-dpo-laser-q4_K_M
ANTHROPIC_API_KEY=
OLLAMA_API_BASE=http://localhost:11434
OLLAMA_API_KEY=

# --- TTS (Texto a Voz con Piper) ---
# Habilitar/deshabilitar TTS
TTS_ENABLED=false
# URL del servicio Piper (Docker: http://piper-tts:5000, local vacío = subprocess)
TTS_URL=
# Voz a utilizar
TTS_VOICE=es_MX/claude/high/es_MX-claude-high

# --- Monitoreo ---
SENTRY_DSN=

# --- Servidor ---
HOST=0.0.0.0
PORT=8000
DEBUG=True

[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    selenium: marks tests requiring Selenium browser (deselect with '-m "not selenium"')
